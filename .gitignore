node_modules
dist
*.tgz
.DS_Store

# Editor MCP config carries credentials inline, and it is the kind of file a
# `git add -A` picks up without anyone reading it. This repo tracks no
# `.vscode/` at all, and this keeps it that way.
.vscode/mcp.json
